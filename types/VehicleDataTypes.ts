export interface IBrand {
    id: string;
    name: string;
}

export interface IModel {
    id: string;
    name: string;
    brand_id: string;
}

export interface IFuelType {
    id: string;
    name: string;
}
